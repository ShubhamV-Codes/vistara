<% layout("/layouts/boilerplate") %>
<div class="row mt-3 show-main">
  <div class="show-heading">
<h3>Listing Details</h3>
</div>

<!-- <div class="card col-6 offset-2">
  <img src="<%= listing.image.url %>" class="card-img-top show-img" alt="listing_image"/>
  <div class="card-body">
    <p class="card-text">
      <b><%= listing.title %></b><br/>
      <%= listing.description %><br/>
      &#8377; <%= listing.price.toLocaleString("en-IN") %><br/>
      <%= listing.location %><br/>
       <%= listing.country %><br/>
    </p>
  </div>
</div> -->
<div class="card col-6 offset-3 shadow-lg mb-4 rounded-4">
  <img src="<%= listing.image.url %>" class="card-img-top show-img rounded-top-4" alt="listing_image"/>

  <div class="card-body text-center">
    <h4 class="card-title mb-2 fw-bold"><%= listing.title %></h4>
    <p class="card-text text-muted"><%= listing.description %></p>

    <div class="mt-3">
      <h5 class="text-success fw-semibold">
        &#8377; <%= listing.price.toLocaleString("en-IN") %> / night
      </h5>
    </div>
    
    <div class="mt-3 text-muted ">
  <p class="mb-1">
    <i class="bi bi-geo-alt-fill text-danger"></i> <%= listing.location %>
  </p>
  <p class="mb-0">
    <i class="bi bi-globe2 text-primary"></i> <%= listing.country %>
  </p>
</div>

    
  </div>
</div>


<br />
<div class="btns d-flex justify-content-center gap-3 mt-3 showbtns">
  <a href="/listings/<%= listing._id %>/edit" class="btn btn-warning px-4">Edit</a>

  <form method="POST" action="/listings/<%=listing._id%>?_method=DELETE">
    <button class="btn btn-danger px-4">Delete</button>
  </form>
</div>


</form>
</div>
